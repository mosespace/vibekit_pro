# Design System Guide

> Stop shipping purple gradient apps. This guide gives you the tools to build unique, professional UIs that do not look like they were generated by the same AI as everyone else's.

---

## Table of Contents
1. [Why AI Design Looks the Same](#why-ai-design-looks-the-same)
2. [The Design System Prompt — Your Most Important Tool](#design-system-prompt)
3. [Choosing a Color Palette](#choosing-a-color-palette)
4. [Typography](#typography)
5. [Component Style Patterns](#component-style-patterns)
6. [Pre-Built Design System Prompts by App Type](#premade-systems)
7. [Using V0 for Design Work](#using-v0)
8. [Design Anti-Patterns Checklist](#anti-patterns)

---

## Why AI Design Looks the Same

Claude Code, V0, and every other AI coding tool was trained on millions of websites. The most common patterns in that training data — purple gradients, blue primary buttons, gray cards with subtle shadows, rounded corners everywhere — become the AI's default preferences.

When you ask Claude Code to "build a beautiful dashboard", it defaults to what it has seen most. The result: an app that looks like every other AI-built app.

The fix is simple: **tell AI exactly what it should build before it makes any decisions.**

A design system prompt locks every visual decision — colors, typography, spacing, component style — before AI writes a single line of CSS. Claude Code then applies those decisions consistently, producing a unique app that reflects your choices instead of its defaults.

---

## The Design System Prompt

This is the most important prompt in your entire build. **Paste this at the start of every new Claude Code session.**

### Template

```
DESIGN SYSTEM — apply every rule below to every component you create or modify.

═══════════════════════════════════════
COLORS
═══════════════════════════════════════
Primary:           [hex — e.g. #0F172A]
Primary foreground: white (#FFFFFF)
Background:        #FFFFFF (light) / #0F172A (dark)
Surface:           #F8FAFC (cards, inputs)
Border:            #E2E8F0
Text primary:      #0F172A
Text secondary:    #64748B
Text muted:        #94A3B8
Destructive:       #EF4444
Success:           #22C55E

═══════════════════════════════════════
TYPOGRAPHY
═══════════════════════════════════════
Font: Inter (via Tailwind)
Heading 1: text-3xl font-semibold text-slate-900
Heading 2: text-2xl font-semibold text-slate-900
Heading 3: text-xl font-medium text-slate-900
Body:      text-base text-slate-700 leading-relaxed
Small:     text-sm text-slate-500
Label:     text-sm font-medium text-slate-700

═══════════════════════════════════════
COMPONENTS
═══════════════════════════════════════
Cards:    bg-white rounded-xl border border-slate-200 shadow-sm p-6
Buttons:  
  Primary:   bg-[primary] text-white rounded-lg px-4 py-2 text-sm font-medium hover:opacity-90
  Secondary: bg-white text-slate-700 border border-slate-200 rounded-lg px-4 py-2 text-sm hover:bg-slate-50
  Ghost:     text-slate-600 hover:bg-slate-100 rounded-lg px-3 py-2 text-sm
Inputs:   border border-slate-300 rounded-lg px-3 py-2 text-sm focus:ring-2 focus:ring-[primary]/20 focus:border-[primary] outline-none
Badges:   
  Default: bg-slate-100 text-slate-700 text-xs px-2 py-1 rounded-full font-medium
  Success: bg-green-50 text-green-700 text-xs px-2 py-1 rounded-full font-medium
  Warning: bg-amber-50 text-amber-700 text-xs px-2 py-1 rounded-full font-medium
  Error:   bg-red-50 text-red-700 text-xs px-2 py-1 rounded-full font-medium
Tables:   
  Header: bg-slate-50 text-xs font-medium text-slate-500 uppercase tracking-wider
  Row: border-b border-slate-100 hover:bg-slate-50/50 transition-colors
  Cell: text-sm text-slate-700 px-4 py-3

═══════════════════════════════════════
SPACING & LAYOUT
═══════════════════════════════════════
Page padding:    p-6 or p-8
Section gap:     space-y-8
Card gap:        space-y-6
Form gap:        space-y-4
Sidebar width:   w-64
Content max-width: max-w-7xl mx-auto

═══════════════════════════════════════
RULES — NEVER BREAK THESE
═══════════════════════════════════════
- NO purple gradients or purple as primary color
- NO neon or very saturated colors
- NO heavy box shadows (shadow-lg or larger) — use shadow-sm only
- NO gradient backgrounds on hero sections (use solid colors)
- Every page must work on mobile (responsive)
- Sidebar collapses to a drawer on mobile
- Consistent border radius: rounded-lg for small elements, rounded-xl for cards
- Icons: use lucide-react only, size-4 or size-5

═══════════════════════════════════════
FEEL
═══════════════════════════════════════
This app should feel like a premium B2B SaaS product.
Reference: Linear, Notion, Vercel Dashboard, Stripe Dashboard.
Not: a colorful consumer app or a landing page template.
```

---

## Choosing a Color Palette

Your primary color choice defines your entire app's personality. Here are proven palettes for common app types:

### Professional / B2B (Dark Navy)
```
Primary:    #1E3A5F  (deep navy)
Accent:     #3B82F6  (blue)
Background: #FFFFFF
Text:       #0F172A
Feel: trusted, enterprise, serious
Best for: dashboards, CRMs, financial apps
```

### Modern / Clean (Slate)
```
Primary:    #0F172A  (near black)
Accent:     #6366F1  (indigo)
Background: #FFFFFF
Text:       #1E293B
Feel: minimal, modern, focused
Best for: productivity apps, dev tools
```

### Energetic / Startup (Orange)
```
Primary:    #F97316  (orange)
Accent:     #0EA5E9  (sky blue)
Background: #FFFFFF
Text:       #1C1917
Feel: energetic, bold, approachable
Best for: consumer SaaS, marketplaces
```

### Nature / Health (Green)
```
Primary:    #16A34A  (green)
Accent:     #0891B2  (teal)
Background: #F8FAF9
Text:       #14532D
Feel: fresh, trustworthy, calming
Best for: health apps, education, sustainability
```

### Premium / Luxury (Deep)
```
Primary:    #1C1917  (warm black)
Accent:     #B45309  (amber)
Background: #FAFAF9
Text:       #1C1917
Feel: premium, exclusive, confident
Best for: high-end services, portfolio, agencies
```

---

## Typography

### The Two-Font System

Every well-designed app uses two fonts: a **display font** (headings, brand moments) and a **body font** (all readable text). Using only one font is fine, but using two creates visual hierarchy.

**Safe single-font choices for Claude Code (already available via Tailwind):**
- `Inter` — clean, modern, highly readable. Best for dashboards and data-heavy apps
- `system-ui` — uses the device's native font. Fastest to load, feels native

**Adding a custom font in Claude Code:**
```
Add the Google Font [font name] to the project.
Import it in the root layout.
Set it as the default font family in tailwind.config.ts.
Use it for all headings (h1, h2, h3) and the brand name.
Keep Inter for body text.
```

### Typography Scale

```
Page title:    text-2xl sm:text-3xl font-semibold leading-tight
Section heading: text-xl font-semibold
Card title:    text-base font-semibold
Body text:     text-sm leading-relaxed (dashboards) / text-base (marketing pages)
Caption:       text-xs text-slate-500
```

---

## Component Style Patterns

Copy-paste these into your design system prompt or your Claude Code prompts for specific components.

### Sidebar Navigation

```
Sidebar style:
- Background: bg-white border-r border-slate-200
- Width: w-64 (fixed, not collapsible on desktop)
- Logo area: h-16 flex items-center px-6 border-b border-slate-200
- Nav items: flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-slate-600
  hover: hover:bg-slate-100 hover:text-slate-900
  active: bg-slate-100 text-slate-900 font-medium
- Icons: lucide-react, size-4, text-slate-400 (inactive) text-slate-700 (active)
- Section labels: text-xs font-medium text-slate-400 uppercase tracking-wider px-3 py-2
```

### Dashboard Header

```
Header style:
- Background: bg-white border-b border-slate-200
- Height: h-16
- Content: max-w-7xl mx-auto px-6 flex items-center justify-between
- Title: text-lg font-semibold text-slate-900
- Right side: flex items-center gap-3
```

### Stats Cards

```
Stats card style:
- Container: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4
- Card: bg-white rounded-xl border border-slate-200 p-6
- Label: text-sm font-medium text-slate-500 mb-1
- Value: text-2xl font-semibold text-slate-900
- Change: text-sm — green if positive (text-green-600), red if negative (text-red-600)
- Icon: bg-[primary]/10 text-[primary] rounded-lg p-2 size-9
```

### Empty States

```
Empty state style:
- Container: flex flex-col items-center justify-center py-16 text-center
- Icon: size-12 text-slate-300 mb-4
- Title: text-base font-medium text-slate-900 mb-1
- Description: text-sm text-slate-500 mb-6
- Action button: primary button style
```

---

## Pre-Made Design System Prompts by App Type

### SaaS Dashboard App

```
DESIGN SYSTEM — SaaS Dashboard

Primary: #1E3A5F | Accent: #3B82F6 | Background: #FFFFFF | Surface: #F8FAFC
Font: Inter | Radius: rounded-xl (cards) rounded-lg (elements)

This is a professional B2B SaaS dashboard — reference: Linear, Notion, Stripe.
NOT a consumer app. NOT colorful. NOT flashy.

Sidebar: w-64 bg-white border-r border-slate-200, nav items with rounded-lg hover states
Header: h-16 bg-white border-b border-slate-200 with page title and user avatar
Content: max-w-7xl mx-auto p-6 to p-8
Cards: bg-white rounded-xl border border-slate-200 shadow-sm
Buttons: bg-[#1E3A5F] text-white rounded-lg (primary), border border-slate-200 bg-white (secondary)
Tables: clean rows, bg-slate-50 header, border-b border-slate-100 rows

NO: purple, gradients, neon, heavy shadows, animations unless functional
```

### E-Commerce Store

```
DESIGN SYSTEM — E-Commerce Store

Primary: #0F172A | Accent: #F97316 | Background: #FFFFFF | Surface: #F9FAFB

Product cards: bg-white rounded-2xl border border-slate-100 overflow-hidden hover:shadow-md transition-shadow
Product image: aspect-square object-cover w-full bg-slate-100
Price: text-xl font-bold text-slate-900
Sale price: text-red-600 font-bold, original price: line-through text-slate-400 text-sm

Cart: fixed right panel, slide-in from right, bg-white shadow-xl
Checkout: clean single-column form, max-w-lg mx-auto

Add to cart button: bg-[#0F172A] text-white w-full py-3 rounded-xl font-medium hover:opacity-90

NO: complex animations, pop-ups, auto-playing anything, cluttered layouts
```

### Landing / Marketing Page

```
DESIGN SYSTEM — Marketing Landing Page

Primary: #7C3AED (purple is acceptable on landing pages — just not everywhere)
Background: #FFFFFF sections alternating with #F9FAFB

Hero: min-h-screen flex items-center, centered content, max-w-4xl
Headline: text-5xl sm:text-6xl font-bold text-slate-900 leading-tight
Subheadline: text-xl text-slate-600 mt-4 max-w-2xl

Features section: grid grid-cols-1 md:grid-cols-3 gap-8
Feature card: rounded-2xl border border-slate-100 p-8 hover:border-[primary]/30 transition

Testimonials: bg-slate-50 section, cards with quotes and avatars
Pricing: clean cards with border-2 on recommended plan

CTA buttons: large, rounded-xl, py-4 px-8
```

---

## Using V0 for Design Work

V0 is better than Claude Code for visual iteration. Use it when you need to try multiple design options quickly.

### The V0 → Claude Code Workflow

1. **Design in V0**: describe what you want visually
2. **Iterate in V0**: regenerate 3–4 times until the design is right
3. **Copy V0 output**: the React component code
4. **Handoff to Claude Code**: paste with this prompt:

```
I designed this component in V0. Here is the code: [PASTE]

Add this to the project at [path]. 
Do NOT change any of the visual design — colors, spacing, typography, layout.
Only add the real data: replace the placeholder text/numbers with actual data from [source].
Wire up the onClick handlers to the appropriate actions.
```

### V0 Design Prompts That Work

```
Design a [component] for a [app type] SaaS application.
Style: clean, professional, similar to [reference app like Linear/Notion/Vercel]
Primary color: [hex]
Use Tailwind CSS only
No purple gradients, no neon colors
Data to display: [describe the data]
The component should feel premium and well-crafted
```

---

## Design Anti-Patterns Checklist

Before shipping any page, check for these:

- [ ] No purple gradients (unless it is a marketing/landing page by choice)
- [ ] No `shadow-lg` or `shadow-xl` on cards (use `shadow-sm` only)
- [ ] No neon colors or very saturated backgrounds
- [ ] No more than 3 distinct colors on any single page
- [ ] Text is always readable on its background (check contrast)
- [ ] Mobile layout does not have text or buttons cut off
- [ ] Consistent border radius throughout (pick rounded-lg OR rounded-xl — not both)
- [ ] All interactive elements have hover states
- [ ] Empty states exist for lists that might have no data
- [ ] Loading states exist for any data that is fetched asynchronously
- [ ] Icons are consistent (all lucide-react, same size family)
- [ ] Sidebar collapses on mobile

---

*Part of the [VibeKit Framework](../README.md) — github.com/MUKE-coder/vibekit*
